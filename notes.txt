git init -- Inicializa el directorio
git status -- ver el estatus del proyecto
git commit -- agrega los archivos crea una foto de los archivos
git checkout -- deshace los cambios del archivo 
git add --agrega archivo al entonce de trabajo
git log --muestras los commit reaizados
git diff -- muestra las diferencias del archivo